class DocumentController {
    constructor(options, callback = (target) => { }) {
        this.initialize();
        this.callback = callback;

        //this.callback(this);
    }

    initialize() {

    }

}