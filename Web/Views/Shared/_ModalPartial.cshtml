<div class="modal fade" id="modalBackdrop" data-backdrop="static" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalLabel">Modal title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body" id="modalContent"></div>
            <div class="modal-footer d-flex justify-content-between">
                <button id="modalSubmitRemoveBtn" type="submit" class="btn btn-danger" form="submitModalRemoveForm" onclick="if (!confirm('Are you sure want to delete this?')) { return false; }" hidden><i class="material-icons mr-1">delete_outline</i> Remove</button>
                &nbsp;
                <div>
                    <button id="modalCloseBtn" type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button id="modalSubmitBtn" type="submit" class="btn btn-primary" form="submitModalFrom" hidden>Save</button>
                </div>
            </div>
        </div>
    </div>
</div>